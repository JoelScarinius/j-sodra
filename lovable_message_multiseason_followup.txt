Use this exact follow-up for the current `/explorer` fixes.

Current status after live manual syncs
- The live 2026 sync was rerun successfully after the 2025 backfill, so the warehouse should now treat `2026` as the current season again.
- If the UI still shows `2025 (current)`, that is now a frontend caching/state issue or the connected project has not reloaded fresh `api_filter_options()` data yet.
- One repo-side bug was identified after the manual backfills: the current `sync-league` code in the repo has now been patched so future older-season backfills do not flip `is_current`, stale placeholder team rows are refreshed, and no-event syncs preserve existing event-derived metrics instead of overwriting them with fake zeros. That updated function still needs to be deployed separately.

Prerequisite before UI validation
- The live Supabase project must first have:
  - the updated `sync-league` edge function deployed
  - the SQL migration `20260529103000_scope_standings_to_team_group.sql` applied
  - one manual previous-season sync run with:
    - `{"provider_league_id":810,"provider_season_ids":[190620],"include_events":false,"detail_concurrency":3,"event_concurrency":1,"hydrate_recent_hours":0}`
  - one final manual current-season sync run with:
    - `{"provider_league_id":810,"provider_season_ids":[192451],"include_events":false,"detail_concurrency":2,"event_concurrency":1,"hydrate_recent_hours":72}`

What must change in Lovable now
- Keep `/` and `/explorer` separate.
- `/` stays on the storage JSON contract and must not reintroduce the old season/analysis scope box.
- `/explorer` must use Supabase RPCs only. Do not read `reports/index.json` or any storage context JSON on `/explorer`.
- On every `/explorer` load, refetch `api_filter_options()` before deciding what season is current.
- Do not label a season as `(current)` from cached local state, previous selections, or route memory. Only append `(current)` when the freshly fetched season row has `is_current === true`.

Required RPC contract for `/explorer`
- Use `api_filter_options()` for selector data.
- Use `api_dashboard_context(season_id, team_id)` for the header, next match, latest match, standing, season-full metrics, and last-five metrics.
- Use `api_standings(season_id, 'current', team_id)`.
  - This is required so parallel North/South tables are scoped to the selected team's subgroup.
  - Do not use the old 2-argument standings call.
- Use `api_team_matches(season_id, team_id, venue_filter, limit, offset)`.
- Use `api_team_metrics(season_id, team_id, venue_filter, match_limit)`.
- Use `api_compare_team_seasons(team_id, [season_id, ...])`.

Field names that must be used exactly
- Team selector rows use `team_id` and `team_name`.
- Header/team identity uses `api_dashboard_context.team`:
  - `name`
  - `short_name`
  - `logo_url`
  - `league_name`
  - `season_name`
- Match cards use:
  - `kickoff_at`
  - `opponent_team_name`
  - `opponent_team_logo_url`
  - `is_home`
- Dynamic metrics and compare rows use:
  - `matches_in_scope`
  - `avg_*`
  - `current_position`

Visible UI rules
- Show human-readable labels only.
- Never display raw ids like `Competition 810` or `Season 192451` in the UI.
- Use `league_name` and `season_name` from the RPC payloads.
- Do not invent or locally rewrite season labels. If the RPC returns `2026`, show `2026`. If it returns `2025`, show `2025`. If it returns a stale `is_current`, refetch the selector payload before rendering.
- If the backend data is still stale after deploy and sync, treat that as a warehouse refresh issue. Do not hardcode label replacements in the UI.
- The standings table must only show the selected team's own subgroup when the competition contains parallel tables.
- Keep the `position` values as the backend returns them. They are league positions, so gaps in the subgroup-scoped table are expected. Do not renumber the visible subset locally.
- Latest match must come from `api_dashboard_context.latest_match` when there is no upcoming match.
- If xP or xT is null, render `Unavailable`.
- If a team-season has no data, show an empty state. Do not reuse data from another season.
- If a team name arrives as a placeholder like `Team 6737`, treat that as a backend data-quality problem. Do not guess or fabricate a nicer team name in the UI.

Specific screenshot issues to correct
- The season dropdown must not show `2025 (current)` if the fresh `api_filter_options()` payload marks `2026` as current.
- The selected season card should use the same `season_name` and `league_name` values as the selector payload and dashboard context.
- The standings table must use `team_name` exactly from the RPC payload; if placeholder rows still appear after a fresh reload, that means the newly patched `sync-league` function still needs deployment and another sync.
- The standings table in `/explorer` is subgroup-scoped, so it can legitimately show non-contiguous league positions like `1, 4, 5, 6, 11...`. That is not a frontend sorting bug unless the table is reordered incorrectly.

What I want you to verify after wiring
1. The header shows `Ettan` and `2026` instead of raw ids.
2. Switching to the previous season shows `2025` instead of a provider id label.
3. The default season after a cold load is the fresh `is_current === true` season from `api_filter_options()`, not a cached older season.
4. The standings table changes with the selected team and does not merge North and South into one table.
5. Latest match is populated and uses the correct opponent name/logo fields.
6. No `/explorer` component reads old storage JSON or renders old season-scope text.